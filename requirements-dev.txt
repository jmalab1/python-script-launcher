# Development-only dependencies (e2e tests and the demo recorder). The
# app itself is pure Go and needs none of these.
pytest>=7.0

# End-to-end browser tests (tests/e2e).
# After installing, fetch a browser: python3 -m playwright install chromium
pytest-playwright>=0.5

# Animated-GIF output for the screencast demo (scripts/dev/make_screencast.py --gif)
pillow>=10.0
