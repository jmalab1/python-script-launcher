# Development-only dependencies. The app itself is pure Go and needs
# none of these.

# The manual dev scripts (scripts/dev/check_file_browser.py,
# scripts/dev/make_screencast.py) drive Chromium through python playwright.
# The test suite itself is Go (make test-e2e).
# After installing, fetch a browser: python3 -m playwright install chromium
playwright>=1.40

# Animated-GIF output for the screencast demo (scripts/dev/make_screencast.py --gif)
pillow>=10.0

# Git pre-commit gate (gofmt/gosec) set up by `make hooks`.
pre-commit>=3.0
